# This file is autogenerated by maturin v1.8.7
# To update, run
##    maturin generate-ci github
name: CI
on:
  push:
    branches:
      - main
      - master # Keep master if you also push to it, otherwise remove
  pull_request: # Keep if you want builds on PRs, otherwise remove
  workflow_dispatch: # Keep for manual runs, if desired

permissions:
  contents: write # Needed for creating and uploading assets to a GitHub Release, and creating tags
  id-token: write # Still needed for OIDC/provenance, etc.
  attestations: write # For build provenance

jobs:
  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
          - runner: windows-latest
            target: x86
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          architecture: ${{ matrix.platform.target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist

  macos:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-13
            target: x86_64
          - runner: macos-14
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Create Auto-Tagged GitHub Release
    runs-on: ubuntu-latest
    # This job runs only on successful pushes to the main/master branch
    if: ${{ (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && success() }}
    needs: [windows, macos, sdist] # Ensure all build jobs complete before proceeding
    permissions:
      contents: write # Needed for creating and uploading assets to a GitHub Release, and creating tags
      id-token: write # For attestations/OIDC
      attestations: write # For build provenance
    steps:
      - uses: actions/checkout@v4 # Needed to get git history for rev-parse

      - name: Get auto-generated version
        id: version
        run: |
          # Generate a timestamped version followed by a short Git commit hash
          RELEASE_VERSION="$(date +'%Y%m%d%H%M%S')-$(git rev-parse --short HEAD)"
          echo "version=${RELEASE_VERSION}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          # Download all artifacts into the current directory.
          # This will create subdirectories like 'wheels-windows-x64/', 'wheels-macos-x86_64/', etc.
          path: .

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          # Automatically creates a new tag if it doesn't exist.
          # Example tag: v20250616111820-abcdef
          tag_name: v${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          body: |
            ## Release ${{ steps.version.outputs.version }}

            This is an automated release from the latest main/master branch.
            Tag: `${{ steps.version.outputs.version }}`
            Commit: `https://github.com/${{ github.repository }}/commit/${{ github.sha }}`

            ---
            *Note: This release was automatically generated by GitHub Actions.*
          draft: false # Set to true to create a draft release instead of publishing immediately
          prerelease: false # <-- Changed this to false to ensure it's always a full release
          files: |
            # Include all wheel artifacts and the sdist.
            # The wildcards ensure we pick up files from the downloaded subdirectories.
            wheels-*/*/*.whl
            wheels-sdist/*.tar.gz
        env:
          # GITHUB_TOKEN is automatically provided by GitHub Actions for repositories.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
